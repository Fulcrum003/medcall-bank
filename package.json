{
  "name": "medcall-bank",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node validate.mjs",
    "prebuild": "node validate.mjs",
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "jsdom": "^25.0.0"
  }
}
